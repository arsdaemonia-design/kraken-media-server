            function playNow(path) {
Html = `<button onclick="event.stopPropagation(); playNow('${escapeStr(heroItem.path)}')" class="bg-white h
                            ? `playNow('${escapeStr(heroItem.path)}')` 
h='${escapeStr(heroItem.path)}'; renderLib();` : `playNow('${escapeStr(heroItem.path)}')`);
                    ? `playNow('${escapeStr(f.path)}')` 
rentPath='${escapeStr(f.path)}'; renderLib();` : `playNow(this.dataset.path)`);
opPropagation(); selectRow(this.dataset.path)' : 'playNow(this.dataset.path)'}">
         onclick="playNow(this.dataset.path)">
            function playNowEncoded(encodedPath) {
                playNow(decodeURIComponent(encodedPath));
         onclick="playNow(this.dataset.path)">